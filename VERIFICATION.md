# 검증 결과 — 2026-09-09

## 2026-09-10 아파트 공개 자료 추가

- 아파트 관련 공개 자료 48종을 익명 요청으로 받았고 실패는 없었습니다. 각 폴더에 manifest와 SHA-256이 있습니다.
- 48종을 `data/apartment_sources.sqlite`에 적재했습니다. 포털이 건수를 따로 알려준 자료는 모두 파일 행 수와 같았습니다. DB 무결성 검사를 통과했습니다.
- K-apt 단지 위치를 같은 단지코드로 보완해 400세대 이상 아파트·주상복합 1,363개 중 1,359개를 위치에 연결했습니다. 이전에는 1,344개였습니다. 남은 4개는 다른 단지와 좌표를 공유해 보류했습니다.
- 공식 좌표와 K-apt 좌표가 750m 넘게 어긋나는 24개는 공식 좌표를 유지하고 표시만 했습니다.
- 장소 검색 기록은 43,809개가 됐습니다. 실행 중인 API에서 고척아이파크MD·래미안옥수리버젠·포레나 당산이 K-apt 위치로 나오는 것을 확인했습니다.
- 적재 도구·아파트·장소 테스트 34개가 통과했습니다. 브라우저 화면 검사는 이번에 다시 하지 않았습니다.
- 2026-09-11: 단지 상세 API와 재건축·재개발 구역 레이어를 추가했습니다. 헬리오시티 상세에서 84개 동·최고 35층·2026년 7월 관리비 27개 항목이 나오고, 대치동 일대 화면에서 구역 63개가 나옵니다. Python 테스트 53개와 타입 검사·빌드가 통과했습니다.
- 2026-09-11 헤드리스 Chrome: '은마' 검색 후 팝업에 K-apt 28개 동·최고 14층, 한국부동산원 28개 동·최고 지상 14층이 나왔고, 구역 레이어를 켜자 58개 구역이 표시됐습니다. 페이지 오류는 없었습니다. 화면: `tests/screenshots/renewal-zones.png`.
- 2026-09-11: 실거래·전월세·공시가격·건축물대장·주택인허가를 같은 필지번호로 단지와 연결했습니다. 잠실엘스는 매매 445건, 2025 전월세 1,924건, 공시가격 5,678호, 인허가 동 74개가 연결됐습니다. 헬리오시티는 자료 간 대표 지번이 달라 연결되지 않았습니다. Python 테스트 56개와 빌드가 통과했습니다.
- 2026-09-11: 자치구 현황·SH·LH·주차 목록(3,117행, 단지 1,583개), 도로명주소 건물DB 공동주택 동(25,040개, 단지 2,878개), 건물에너지(전기 2,954·가스 2,677 단지)를 도로명주소 건물DB의 정확한 주소 일치로 연결했습니다. 주소 해석 테스트 4개를 추가했습니다.
- 2026-09-11: 정비사업 정보몽땅 사업장(223행)·도시정비 통계(111행)와 SH 임대 단지 상세 909쪽(주소 909개, 582행 연결)을 추가 연결했습니다. 목록 연결은 모두 4,033행, 단지 1,734개입니다. 개포주공6단지 팝업에 재건축 사업장·도시정비 통계·주차면수가 함께 나오는 것을 API로 확인했습니다.
- 자세한 내용: [자료 목록](docs/APARTMENT_SOURCES.md), [적재 결과](docs/APARTMENT_SOURCES_IMPORT.md), [400세대 이상 단지](docs/APARTMENTS_400.md).

검증 대상은 로컬 지도 `http://127.0.0.1:4173/`와 API 8791입니다. 외부 배포·GitHub 업로드·Supabase 연결은 하지 않았습니다.

**현재 변경 검증:** 자동 사람·차량 이동을 제거하고 **사람 1인칭**·선택적 정적 크기 비교 모형으로 전환했습니다. Node 39개·Chrome 전체 26개·TypeScript/Vite 운영 빌드가 통과했습니다. 진입 직후 이전 카메라 행렬을 쓰지 않도록 보완한 뒤 정적 모형·눈높이·확대 회귀 11개도 다시 통과했습니다. 아래 이전 통과 수치는 현재 결과로 합산하지 않습니다.

## 이번 사람 1인칭·정적 모형 변경

- 자동 이동 컨트롤러·빠른 이동 토글·이동 상태 배지를 앱에서 제거합니다. 일반 지도의 최대 줌에서도 자동 경로 요청이나 사람·차량 표시가 생기지 않도록 검사합니다.
- 1인칭에 진입한 동안에만 최대 사람 1개·승용차 1개의 고정 크기 비교 모형을 표시합니다. 사람 높이 1.7m, 차량 길이 4.3m·폭 1.8m·높이 1.5m이며 실제 관측 위치가 아닙니다. 자료·가림 조건에 따라 모형이 없을 수 있습니다.
- 같은 장면의 모형 세계 좌표는 수동 3m 이동·회전·표시 토글에도 유지합니다. 모드 종료·자유 시점 전환·진입 실패 때 비웁니다. 정적 컨트롤러는 경로 조회·타이머·RAF·지도 재렌더 요청 루프를 만들지 않습니다.
- 기존 1.7m 카메라·지형 1배·확대/휠/드래그 유지·이전 시점 복원·모바일 회귀는 유지하고 버튼명을 **사람 1인칭**과 **사람 1인칭 예시**에 맞췄습니다.
- `tests/mobility.spec.ts`를 정적 모형 검사 4개로 교체했습니다. 실행 결과와 화면은 `tests/screenshots/scale-references-verification.json`과 `first-person-static.png`에 저장했습니다.

행당동 1440×960의 추가 측정에서 사람 1개·차량 1개, 눈높이 약 1.699963m, 2초간 추가 지도/모형 프레임 각각 0, 수동 이동 후 고정 좌표 유지, 종료 시 모형 제거, 페이지 예외 0을 확인했습니다. [현재 정적 측정](docs/FIRST_PERSON_MEASUREMENTS.json).

운영 빌드 재시작 후 Chrome에서 **사람 1인칭** 진입, 모형 캔버스의 1.2초 픽셀 불변, 진입 전 경로 요청 0·진입 후 1회, 개발 hook 없음, 로드된 JS/CSS 5개 gzip, 페이지 예외·로컬 HTTP 오류 0을 확인했습니다. 실제 Chrome 탭도 새로고침해 1인칭 예시를 열었습니다. [운영 확인](tests/screenshots/first-person-production.json), [운영 화면](tests/screenshots/first-person-production.png).

구현 범위: [사람 1인칭](docs/MOBILITY.md), [성능 정책](docs/PERFORMANCE.md).

## 직전 버전 검증 — 정적 모형 전환 전

연한 도로·확대 되돌림 수정 후 Node 회귀 48개, Chrome 전체 25개 통과. 이후 모드 전환·눈높이 재요청 실패 복구를 보완하고 관련 Chrome 7개를 다시 통과했습니다. TypeScript / Vite 운영 빌드도 통과했습니다.

### 연한 도로와 확대 되돌림 수정

- 이전 버전에서 기본 확대 버튼을 눌렀을 때 줌 22.689→20.080, 지형 1→4배 변화를 재현했습니다. 수정 후 같은 버튼으로 22.689→23.689가 유지됩니다. 축소를 4회 눌러 20.5 경계를 넘어도 지형 1배를 보존합니다.
- 휠·드래그·카메라 API 조작 후 idle이 확대 수준과 좌표를 덮어쓰지 않는지 확인했습니다. 지형 타일 갱신은 수직 고도만 보정합니다.
- 2D/2.5D 전환은 현재 좌표·줌을 유지합니다. 눈높이 재요청 실패도 그 요청 직전 시점으로 복구합니다. 명시적 '지도 시점' 버튼만 해당 눈높이 진입 전 시점을 복원합니다.
- 도로는 #e1e5e8 연한 회색 중심선이며 최대 12px, 보행·횡단·계단은 3–3.5px입니다. 확대 시 거대해지던 2,080px 폭과 폭 배수 점선을 제거했습니다. 실제 도로 폭을 재현한 도형은 아닙니다.
- 줌 19 이상·기울기 75° 이상에서 등고선·표고점·이전 높이 조회 점선·힐셰이드·건물 테두리를 숨깁니다. 로컬 도로가 준비되면 중복 배경 도로와 도로 이름도 숨깁니다. 설정의 체크값과 원본 자료는 보존하며 일반 보기에서 복원됩니다.
- 운영 빌드에서도 실제 확대·축소 버튼 후 지형 1배가 유지됐습니다. 페이지 예외와 로컬 HTTP 오류는 0이며, 개발 hook 없이 JS·CSS·worker를 gzip으로 제공합니다. 기존 사용자 Chrome 탭도 새 버전으로 열었습니다.
- [확대 버튼 실측](tests/screenshots/zoom-button-regression.json), [회귀 전후](docs/ZOOM_REGRESSION.json), [거리 화면](tests/screenshots/pale-street.png), [동네 화면](tests/screenshots/pale-neighborhood.png), [운영 확인](tests/screenshots/pale-street-production.json).


- 일반 줌 17·19·20·20.49: 이동 API 요청 0, 이동 프레임 증가 0.
- 실제 경로 앵커와 현재 `map.project` 화면 위치 오차 0px. 사람·차량 이동 동안 지도/Three 렌더 프레임 증가 0.
- 최대 줌 24·기울기 85°, 지형 타일 전환 후 눈높이 1.7m 유지, 보는 방향으로 3m 이동, 좌우 회전과 기존 모드·배율·카메라 복원 확인.
- 360px 모바일에서 눈높이/이동 버튼 가림·가로 넘침 없음. 눈높이 API 실패 시 이전 2D 모드 보존.
- 동일 행당동 장면 최초 사람·차량 배치 계산424.6→103.5ms. 10회 표본의 그리기0.7–4.9ms, 눈높이1.699963m, 지도 프레임 증가0. [당시 측정](docs/STREET_VIEW_MEASUREMENTS.json).
- 운영 빌드도 실제 Chrome에서 눈높이 진입·3m 이동·캔버스 픽셀 변화 확인. 페이지 예외0, 로컬 HTTP 오류0, 개발 hook 없음, JS/CSS/worker gzip 확인. 기존 사용자 Chrome 탭을 새 운영 화면으로 다시 열었습니다. [운영 확인](tests/screenshots/street-eye-production.json).
- 당시 증거: [눈높이 카메라](tests/screenshots/street-eye-verification.json), [이동 지면 정합](tests/screenshots/mobility-verification.json), [화면](tests/screenshots/street-eye.png), [모바일](tests/screenshots/street-eye-mobile.png).

아래 이전 검증 기록의 API 데이터 감사·압축 전송 수치는 그때 측정한 값입니다.

## 이전 근접 보기 구현 — 자동 이동 제거 전

당시에는 줌 20.5 이상에서 사람·차량을 자동 표시하고 최대 15fps·24명/12대(가볍게 12명/6대), 400m 창·50m 격자·3영역 캐시를 사용했습니다. 경사·땀 효과와 함께 제거된 기능입니다. 위의 이동 캔버스 프레임·배치 시간·이전 스크린샷은 이 버전의 역사적 측정이며 현재 정적 모형에 적용하지 않습니다.

1.7m 카메라, 최대 줌 24·기울기 85°, 지형 1배, 지면 앵커, 보수적 가림 정책, 근접 상세 자료의 중심 ±300m 전체 정사각형 조회는 현재 기능에도 남아 있습니다. 이번 실행 결과가 기록되기 전에는 이전 화면만으로 재검증 완료를 주장하지 않습니다.

## 이전 전체 검증 — 눈높이·근접 보기 변경 전

| 검사 | 결과 |
|---|---|
| Python API·원본·가져오기·압축 HTTP·이동 경로 검사 | 75개 통과 |
| Node GLTF·나무·데이터 캐시·경사·애니메이션 중단 검사 | 33개 통과 |
| Playwright Chrome | 전체 18개 시나리오 단일 실행 통과 (58.2초) |
| TypeScript / Vite build | 통과 |
| 운영 빌드 Chrome 검사 | 페이지 예외0 / 로컬 HTTP 오류0 / 개발 hook 없음 |
| 운영 압축 전송 | JS·CSS·MapLibre worker·나무 worker·실제 GLB gzip 확인 |

이전 운영 빌드에서 사람30명·차량23대·땀8명 표시, 800ms 사이 실제 캔버스 픽셀 변화, 페이지 예외0·로컬 HTTP 오류0·개발 hook 없음, 이동 API와 모든 로드된 JS/CSS의 gzip을 확인했습니다. [이전 운영 이동 검사](tests/screenshots/production-mobility-verification.json). 이 인원·성능 수치는 현재 근접 보기 상한과 비교하지 않습니다.

## 이전 구현에서 확인한 동작과 자료

이전 이동 표현은 줌16.2 이상에서 작동했습니다. 원래 OSM 보행로·계단·차도와 원배율 보간 DEM을 사용하며, 오르막15° 또는 개별 길 구간의 연속 오르막200m·상승10m 조건을 검사했습니다. 역방향 경사, 15%와15° 구분, 평지·내리막 초기화, 일방통행, 지형 결측과 표본 사이 결측 구멍, 에스컬레이터 제외도 검사했습니다. 경로 자료의 고도·결측 필터는 1인칭 카메라에 재사용하지만 땀·속도 표현은 제거했습니다. [현재 범위](docs/MOBILITY.md).

이전 Chrome 예시에서 사람30명·차량23대·땀8명을 그렸습니다. 2초 측정 동안 이동 캔버스14프레임, 3D 지도 추가 프레임0을 확인했습니다. 당시 상한은20fps·기본48명/24대·가볍게24명/12대였습니다. OFF·축소 시 프레임 중단, 차도·보행로·계단과의 연동, 요청 실패 시 지도 유지와 실제 캔버스 픽셀도 검사했습니다. 탭 숨김·지연 지형 행렬·늦은 응답 취소는 별도 제어 회귀로 확인했습니다. [이전 화면](tests/screenshots/mobility-uphill.png), [이전 원시 측정](tests/screenshots/mobility-verification.json).

건물·도로 렌더용 속성을 줄여 세 실제 영역에서 worker에 넘기는 JSON 바이트가42.89–45.47% 감소했습니다. 원본 좌표·상세 속성은 유지합니다. 검은 차도 중복 레이어 제거, 반복 필터·setData 생략, 완전한 범위 응답만 재사용하는 캐시도 검사했습니다. [재현 가능한 측정](docs/MAP_RENDER_MEASUREMENTS.json).

고정 동네 버튼·큰 지역명 카드·배경 동네 이름 레이어를 제거했습니다. 이름 검색과 위치 선택 안내는 유지됩니다. 360/390px 화면에서 빠른 표시 버튼과 설정 패널 조작을 확인했습니다.

서울시 공동주택 CSV 2,887행·47열을 전수 보존했습니다. 아파트·주상복합으로 등록되고 전체세대수가 400 이상인 단지 코드 1,363개 중 1,344개를 위치에 연결했습니다. 원본 세대수0/빈값, 임대·분양 코드 구분, 다른 주소의 좌표 중복, 구 경계 불일치 등을 검사했습니다. 19개는 위치 미해결 상태이며 임의 좌표를 만들지 않았습니다. 전체 검색 DB는43,757개입니다. [자료와 검증](docs/APARTMENTS_400.md).

브라우저에서 헬리오시티9,510세대의 공식 코드·출처를 확인하고 강남역 및 개별 건물 상세 검색을 검증했습니다. 400세대 필터는 주거 핀에만 적용됩니다. 단지 위치·세대수를 개별 건물의 높이·공식 주소 연결과 혼동하지 않습니다.

녹지 레이어를 실제 landcover 분류에 맞춰 수정하고 숲·공원·잔디를 녹색으로 구분했습니다. 남산 숲에 장식 나무240개가 실제 WebGL로 그려지며 나무·건물 표시를 독립적으로 끌 수 있습니다. 나무의 개별 위치·높이는 측량 자료가 아닙니다. 숲·구멍·건물·도로·조회 경계 회피는 geometry 테스트로 검사했습니다. [녹지](docs/GREENERY.md).

기존 서울 비행 프로젝트의 Blender 모델4개를 실제 GLTFLoader로 파싱하고 SHA·바닥 원점·물리 치수를 대조했습니다. 네 위치 모두 실제 Chrome에서 렌더링한 후 주변 건물/도로 응답과 타일이 준비된 전체 실루엣을 촬영했습니다.

- 롯데월드타워555m / 63빌딩249.58m / N서울타워236.7m / 트레이드타워 끝256.5m
- 지형4→1배에서 바닥 고도만1/4, 모델 높이는 동일
- 모델OFF·2D·실패 시 원본 일반 건물 입체 복원
- 동일 GLB는 위치를 오가도 각각1회 요청
- 기존GLB는 원본 그대로이며, 이번 새 Blender 재수출은 macOS Metal 초기화 오류로 실패. 참고 재현인 세부 외형·평면 치수는 공식 CAD 정확도를 주장하지 않음

[모델과 출처](docs/CITY_MODELS.md), [실제 브라우저 렌더 증거](tests/screenshots/city-model-render-proof.json).

## 성능

아래 HTTP·idle 브라우저 수치는 근접 보기 변경 전 측정 기록입니다. 현재 정적 모형의 프레임 비용을 측정한 결과는 아닙니다. 렌더 속성 경량화의 [MAP_RENDER_MEASUREMENTS.json](docs/MAP_RENDER_MEASUREMENTS.json)은 로컬 API와 컨트롤러 출력으로 재현하며, 현재 측정에서도 기존 여섯 바이트 값과 원본 geometry 보존이 일치했습니다.

코드를 minify·분할하고 정적 gzip 파일을 생성했습니다. API도 압축을 협상하며 평문과 압축 해제 후 바이트 일치를 확인했습니다. 실제 상도 나무 제외 도형은 기존 전체 속성 응답 약2.81MB에서 필요한 geometry 약1.07MB, HTTP gzip274,801B로 줄었습니다. 모델 지연 로드, 제한된 제외 도형/나무 결과 캐시, 진행 중 요청 공유, 나무 계산 Worker, 인스턴스 버퍼 재사용을 적용했습니다.

DPR2 브라우저에서 기본pixel ratio1.25 및 수동1/2 변경을 검사했습니다. 기본 캔버스 픽셀 수는 DPR2 그대로일 때의39.1%이며 이는 FPS 개선율이 아닙니다. 나무 토글 후 같은 영역을 다시 요청하지 않는 것도 확인했습니다.

초기 map.loaded() 다음에도 예약된 나무 계산·지도 라벨 fade가 남는 현상을 분리했습니다. repaint 호출 stack을 수집해 첫 정착 구간 이후 연속4초에 각0프레임·추가 API 요청0임을 확인했습니다. 최종 회귀에서는 네트워크·worker·프레임의 정착을 확인한 다음 별도1초를 측정하여0프레임을 확인했습니다. 일정 FPS·모든 기기·장시간 메모리 감소율은 측정하지 않았습니다. [정책 및 수치](docs/PERFORMANCE.md).

## 원본 자료 보존

- 등고선8,570개·표고점45,870개, 등고선 모든4,413,952좌표와 표고점 좌표·HEIGHT를 원본과 대조해 불일치0. 지형SQLite114,352,128B. [전수 감사](docs/CONTOUR_AUDIT.md).
- 2.5D는726,207제약점의 보간 결과이며818개 z10–14 DEM·mask·coverage 타일. 원본의 모든 선·점 보존과 모든 지점의 실측 고도 확보는 다릅니다.
- 건물365,456동+부분1,684개. 건물26,626동/부분504개의 기록 높이가 있고 나머지는 임의 높이 없이 외곽선으로 유지합니다. OSM 기여 기록이며 모든 건물의 공식 대장 높이를 연결한 상태는 아닙니다.
- 건축물대장 표제부 CSV585,194행·62열을 확보했으나 좌표/공통 키 부족으로 일반 건물 도형에 미연결. [대장 감사](docs/REGISTER_CSV_AUDIT.md).
- 차도79,812선·보행로37,071선·계단3,034선, 원본725,816좌표의 DB 불일치0. 원본 OSM 기준2026-06-01. [도로](docs/ROAD_SOURCES.md).
- 테스트 북마크는 정리했으며 사용자 북마크는 변경하지 않았습니다. 새 데이터 취득에 유료API·BrightData를 사용하지 않았습니다.

## 화면 증거

다음 파일은 정적 모형 전환 전 화면 증거입니다. 자동 이동이 보이는 파일은 과거 동작이며 새 사람 1인칭 화면의 최종 결과는 별도로 추가합니다.

- [운영 이동·땀 효과](tests/screenshots/production-mobility.png), [정지 지도와 이동 캔버스 분리 측정](tests/screenshots/mobility-verification.json)
- [운영 빌드 녹지·모델](tests/screenshots/production-greenery-models.png), [운영 HTTP/브라우저 결과](tests/screenshots/production-verification.json)
- `tests/screenshots/city-model-lotte.png`, `city-model-sixtythree.png`, `city-model-nseoul.png`, `city-model-coex.png`
- `tests/screenshots/namsan-greenery.png`, `helio-landmarks.png`, `road-walkway-layers.png`, `mobile-25d.png`
