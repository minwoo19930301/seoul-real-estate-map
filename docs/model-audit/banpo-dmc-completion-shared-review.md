# 기존 대표 두 개로 23동 추가

2026-09-22. 반포자이 103동과 DMC파크뷰자이의 남은 22동에 이미 검토된 대표 GLB를 복제했다. 새 대표 모델·Blender 파일·동별 사진·렌더는 만들지 않았다.

| 단지 | 이번 추가 | 누적 위치 적용 | 공유 대표 |
| --- | ---: | ---: | --- |
| 반포자이 | 1동 | 44/44동 | 기존 101동 |
| DMC파크뷰자이 | 22동 | 61/61동 | 기존 301동 |

반포 103동은 대장 27층·57.7m와 높이가 없는 원천 자료의 충돌이 남아 있다. 새 높이를 확정하지 않고 현재 표시하던 fallback GLB의 실제 높이 82.35m와 앵커를 유지했다. 이는 기존 27층×3.05m 추정값이며 실측 높이가 아니다. 대장 수치와 미확정 표시는 함께 보존한다. 대표 101동의 29층·24층 날개 패턴이 반복되므로 103동의 층별 재구성으로 보지 않는다.

DMC 추가 22동은 이름이 있는 OSM 단지 경계 다섯 개, 정확한 도로·지번·동번호, 주건축물 아파트 대장과 원천 geometry·properties를 독립 확인했다. 새 후보는 모두 층수가 일치하고 높이 차이가 0.4m 이내다. 기존 39동과 합쳐 대장의 숫자 주거동 61개에 대응한다. 기존 302동은 OSM 경계 일부를 넘지만 이미 게시한 식별·배치를 유지했다. OSM 경계는 지적 측량 자료가 아니다.

## 원천 보존과 표시 수정

기존 복합 모델 네 개에서 삼각형 24,214개와 모든 정점 속성을 제작 순서로 검증해 나눴다. 이전에 한 번 분리했던 residual 한 개는 원본을 재생하고 현재 파일 바이트까지 일치시킨 뒤 새 파일로 다시 나눴다. 주변 47개 source를 보존하며 이전 파일을 덮어쓰지 않는다.

재분할 뒤 residual ID가 바뀌면 모델 표시 수 제한에서 주변 건물의 우선순위가 사라지던 문제도 고쳤다. 런타임에서 원본 연결을 이어받아 처리하며 공개 correction 기록은 수정하지 않는다. 반복 재분할, 기존 target 보존, 48개 제한, 대표 로딩 실패를 회귀 검사했다.

기준 d1be189의 bespoke 887개·reference 125개·generic 13,607개 기록과 correction 48개, GLB·blend 114,757개 파일을 그대로 보존했다. 현재 공유 복제는 467동, 대표 외관 추정은 828동이다. 대상 1,417개 관리코드 중 식별된 주거동 전체 적용은 29개다. 서울 전수 완료를 뜻하지 않는다.

## 검증

Node 13,886개, Python 68개, 수정 후 브라우저 6개 시나리오와 빌드를 통과했다. 정상 표시·대표 실패·대표와 fallback 동시 실패 후 원천 복구를 확인했다. 본체 배치 검사에서 겹침이 없다. 검사 범위 밖 장식이나 모든 이웃 건물까지 검증한 것은 아니다. generic-corrections 청크 크기 경고는 약 2,970KB / gzip226KB로 남아 있다.

[반포 지도](renders/shared-apartment-instances/banpo-xi-final-none.png) · [DMC 지도](renders/shared-apartment-instances/dmc-parkview-xi-completion-none.png) · [분리 증거](dmc-completion-generic-split.json) · [로컬 검증](banpo-dmc-completion-local-validation.json)

PR로 게시한다. 병합·배포 상태는 별도다.
