# 금호파크힐스·옥수리버젠·DMC파크뷰자이 대표 공유

2026-09-22. 세 단지의 대표 모델을 하나씩 만들고 나머지 34동은 해당 GLB의 메시와 재질을 공유해 배치했다. 복제별 사진 조사나 새 모델·4방향 렌더는 만들지 않았다.

| 단지 | 대표 | 현재 적용 | 남은 대응 |
| --- | --- | --- | --- |
| e편한세상금호파크힐스 | 101동, 등록 19층·54.25m | 대표 1 + 복제 15 = 16/17동 | 1동 미확정 |
| 래미안옥수리버젠 | 101동, 등록 12층·35m | 대표 1 + 복제 14 = 15/15동 | 식별된 주거동 전체 적용 |
| DMC파크뷰자이 | 301동, 등록 20층·57.6m | 대표 1 + 복제 5 = 6/61동 | 55동 원천 위치·번호 미확정 |

사진의 동 번호와 방위가 확인되지 않아 단지 공통 외관을 대표동에 적용한 추론으로 기록했다. 금호는 밝은 창 격자, 회색 서비스 벽과 전층 황갈색 끝벽을 반영했다. 최초 작은 황갈색 장식만 있던 안은 사진 대조 후 넓은 불투명 끝벽으로 고쳤다. 옥수는 밝은 긴 창면과 넓은 끝벽, 상부 두 줄 띠와 회색 저층부를 반영했다. 사진 왼쪽 극동2 건물은 포함하지 않았다. DMC는 여러 층의 창 묶음을 둘러싸는 주황 프레임, 밝은 벽과 흰 창틀, 뒤로 물러난 상부 회색 루버를 반영했다. 작은 옥상 상자로 처리했던 첫 안은 실제 상부 입면의 개구부로 수정했다.

대표는 원천 평면과 등록 높이를 사용한다. 복제는 그 대표의 평면·창문·층 패턴을 반복하고 위치·방향·크기를 바꾼다. 각 동의 실제 오목한 윤곽과 층별 차이, 가려진 입면이나 지붕 치수를 재현한 것은 아니다.

## 원천과 기존 모델 보존

숫자만 있는 원천 이름과 `동` 접미사가 있는 대장을 정규화해 대조했다. 정확한 도로 주소와 지번, 같은 층수와 1m 이내 높이 차이, 공식 좌표 인근의 원천을 사용했다. 실제 SQLite geometry·properties·대장 원문을 독립 확인했다. 공식 단지 경계도에 의한 귀속 확정은 아니며 복합 모델에 함께 묶여 있다는 이유만으로 같은 단지로 보지 않는다.

금호 공식 세대수는 서울시 자료 1,467과 K-apt 1,330으로 달라 두 값을 모두 보존했다. 두 자료 모두 400세대 이상이다. DMC의 다른 주소나 불명확한 원천은 이번 적용 대상에 포함하지 않았다.

기존 세 복합 모델의 33,787개 삼각형을 원본 제작 순서로 재실행해 바이트 일치를 확인한 뒤 분리했다. 37개 대상 외 주변·미확정 원천 여섯 개와 지형 앵커, 재질, 정점 속성을 보존했다. 기존 모델의 단순화된 볼록껍질 근사도 그대로 남긴다.

## 검증

- Node 13,852개, Python 55개, 브라우저 9개 시나리오와 프로덕션 빌드 통과.
- 세 단지의 정상 표시, 공유 파일 실패, 공유 파일과 개별 fallback 동시 실패 후 원천 지도 복구를 확인했다. 대표 파일 요청은 한 번이고 복제 geometry/material은 동일하다.
- 원본 사진, 대표 네 방향 렌더와 세 단지 지도 화면을 검토했다. 이번 34개 복제 본체와 식별된 같은 단지 원천 평면 사이 겹침 0건. 모든 주변 건물이나 장식 돌출까지 검증한 것은 아니다.
- 기준 커밋 02a50569f의 bespoke 690개, reference 125개, generic 13,607개 기록, 교정 35개와 GLB·blend 114,553개 파일 바이트 보존 확인.
- 현재 bespoke 727개 기록에는 과거 보존본이 포함된다. 현행 공유 복제 290동, 대표 외관 추정 645동이다. 대상 1,417개 관리코드 중 주거동 전체 적용은 26개이며 서울 전수 완료를 뜻하지 않는다.
- generic-corrections 청크가 약 2,304KB, gzip 약 173KB로 빌드 크기 경고는 남아 있다.

[금호 지도](renders/shared-apartment-instances/geumho-park-hills-none.png) · [옥수 지도](renders/shared-apartment-instances/raemian-oksu-riverzen-none.png) · [DMC 지도](renders/shared-apartment-instances/dmc-parkview-xi-none.png) · [분리 증거](geumho-oksu-dmc-generic-split.json) · [로컬 검증](geumho-oksu-dmc-local-validation.json)

PR·CI·병합·배포는 별도 상태다.
